<div class="navbar navbar-default navbar-static-top">
  <div class="container">
    <button class="navbar-toggle" data-target=".navbar-responsive-collapse" data-toggle="collapse" type="button">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>

    <% if current_employee %>
      <%= link_to 'Task Manager | tecnical test for TODOSISTEMAS', authenticated_root_path, class: ['navbar-brand'] %>
    <% else %>
      <%= link_to 'Task Manager | tecnical test for TODOSISTEMAS', unauthenticated_root_path, class: ['navbar-brand'] %>
    <% end %>

    <div class="navbar-collapse collapse navbar-responsive-collapse">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to 'Create task', new_task_path if employee_signed_in? %>
        </li>
        <li>
          <%= link_to 'Edit profile', edit_employee_path(@employee) if current_employee == @employee %>
        </li>
        <li>
          <%= link_to 'Logout', destroy_employee_session_path, method: :delete if employee_signed_in? %>
        </li>
      </ul>
    </div>
  </div>
</div>